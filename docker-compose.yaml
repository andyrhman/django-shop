services:
  shop_monolith:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    container_name: shop_monolith
    volumes:
      - .:/app
      - /home/tataran/TES/aiven-kafka:/certs:ro
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SSL_CAFILE: /certs/ca.pem
      SSL_CERTFILE: /certs/service.cert
      SSL_KEYFILE: /certs/service.key
    networks:
      - monolith
      - users
networks:
  monolith:
  users:
    external: true
